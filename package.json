{
  "name": "generativeai",
  "description": "1. Upload and read pdf: [use azure cognitive services (Computer Vision API) or nodejs library to convert to text]\r 2. New account for openai, keys are already expired [Only if Palak's Keys don't work]\r 3. Preprocess the text file in the backend [Partially done in python code]\r 4. Create prompt\r 5. Create token thing, temp < 0.5\r 6. Sending token and process the response token\r 7. Integrate with CLI\r 8. Make it interactive and enhance UX.",
  "version": "1.0.0",
  "main": "DataProcess.js",
  "dependencies": {
    "azure-search": "^0.0.21",
    "azure-storage": "^2.10.7",
    "dotenv": "^16.1.4",
    "ms-rest-azure": "^3.0.2",
    "node": "^20.2.0",
    "pdfreader": "^3.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "keywords": []
}
